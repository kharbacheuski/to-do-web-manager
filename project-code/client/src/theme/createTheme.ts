import { createTheme, colors} from "@mui/material"

export const theme = createTheme({
    palette: {
        primary: {
            main: '#1e1e1e',
            dark: '#000000',
            contrastText: '#ffffff',
        },
        secondary: {
            main: '#dc3535',
        },
    },
})